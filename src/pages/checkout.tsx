import { NextPage } from "next";
import React from "react";
import { Layout } from "../components/Layout";
import { Wrapper } from "../components/Wrapper";

const Checkout: NextPage = () => {
  return (
    <Layout>
      <Wrapper variant="wide"></Wrapper>
    </Layout>
  );
};

export default Checkout;
